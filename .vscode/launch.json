{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C Project",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "preLaunchTask": "build_c_project",
            "externalConsole": true
        }
    ]
}
